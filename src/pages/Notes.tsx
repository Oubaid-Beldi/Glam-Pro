import { StickyNote } from 'lucide-react'
import { PlaceholderPage } from '@/components/PlaceholderPage'

export default function Notes() {
  return (
    <PlaceholderPage
      title="Notes"
      icon={StickyNote}
      description="Capture ideas and reference material for the selected project."
    />
  )
}
